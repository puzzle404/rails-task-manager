
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Volver al inicio", tasks_path %> ||
<%= link_to "Borrar", task_path(@task), method: :delete, data: { confirm: ":Are you sure?" }%>
<%= link_to "Edit", edit_task_path, method: :get %>
